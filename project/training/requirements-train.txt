# Training environment for the router classifier.
#
# Intended for:
# - prepare_dataset.py
# - train.py
#
# Install flow:
#   1. Check driver / toolkit:
#        nvidia-smi
#        nvcc --version
#   2. Install a PyTorch wheel first:
#        pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121
#   3. Install this file:
#        pip install -r requirements-train.txt
#
# Notes:
# - This environment intentionally excludes vLLM.
# - This path uses plain Transformers + TRL + PEFT, no Unsloth.
# - FlashAttention is not required for first-pass classifier training.

# Core training stack
#
# These are pinned on purpose.
# Loose ranges caused pip to backtrack into incompatible PEFT /
# Transformers combinations on the VM.
transformers==4.41.2
datasets==2.20.0
trl==0.9.6
accelerate==0.31.0
peft==0.11.1

# Utilities
huggingface-hub==0.24.6
sentencepiece==0.2.0
protobuf==5.27.3
