# Serving environment for the fine-tuned router classifier.
#
# Intended for:
# - serve.py
# - integrate.py
#
# Install flow:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-serve.txt
#
# Notes:
# - vllm dropped Python 3.9 support; we use plain transformers inference instead.
# - This is sufficient for a small Qwen2-0.5B / 1.5B classifier.
# - transformers pinned to match the version used during training.

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
transformers==4.41.2
sentencepiece==0.2.0
protobuf==5.27.3
accelerate>=0.31.0
